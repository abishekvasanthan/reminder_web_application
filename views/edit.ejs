<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Form</title>
</head>

<body>
    <!DOCTYPE html>
    <html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>New Todo</title>
        <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css" type="text/css" />
        <link rel="stylesheet" href="/stylesheets/edit.css" type="text/css" />
    </head>

    <body>
        <div class="ui fixed inverted menu">
            <div class="ui container">
                <div class="header item"><i class="edit icon"></i>Todo Site</div>
                <a href="/" class="item">Home</a>

                <div class="right menu">
                    <% if(!currentUser){ %>
                        <div class="item">
                            <a href="/login" class="ui green basic button">Login</a>
                        </div>
                        <div class="item">
                            <a href="/register" class="ui primary button">Sign Up</a>
                        </div>
                        <% }else{ %>
                            <div class="item">
                                <button class="ui labeled icon inverted yellow button">
                                <i class="user circle outline icon"></i>
                                <%= currentUser.username %>
                                </button>
                            </div>
                            <div class="item">
                                <a href="/logout" class="ui inverted red button">Logout</a>
                            </div>
                            <% } %>
                </div>
            </div>
        </div>
        <div class="ui main text container segment">
            <div class="ui huge header">Edit
                <%= editTodo.title %>
            </div>
            <form class="ui form" action="/todos/<%= editTodo._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                <div class="field">
                    <label>Title</label>
                    <input type="text" name="todo[title]" value="<%= editTodo.title %>">
                </div>
                <div class="field">
                    <label>Image</label>
                    <input class="form-control-file" type="file" name="photo">
                </div>
                <div class="field">
                    <label>Date</label>
                    <input class="form-control ui calender" name="todo[date]" type="date">
                </div>
                <div class="field">
                    <label>Time</label>
                    <input class="form-control ui calender" name="todo[time]" type="time">
                </div>
                <div class="field">
                    <label>Body</label>
                    <textarea name="todo[body]"><%= editTodo.body %></textarea>
                </div>
                <input class="ui violet inverted button" type="submit">
            </form>
        </div>

    </body>

    </html>
</body>

</html>